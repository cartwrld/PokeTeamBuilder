

class PokeStats {
  constructor(stats) {
    this.hp = stats[0];
    this.attack = stats[1];
    this.defense = stats[2];
    this.specAtk = stats[3];
    this.specDef = stats[4];
    this.speed = stats[5];
  }
}
